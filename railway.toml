[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -jar target/qualifier-1.0.0.jar"
healthcheckPath = "/bfhl/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
